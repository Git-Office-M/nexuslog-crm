# ============================================================
# NEXUS LOG — Variáveis de Ambiente
# ------------------------------------------------------------
# 1. Copie este arquivo: cp .env.example .env.local
# 2. Preencha com os valores do seu projeto Supabase
# 3. NUNCA faça commit do .env.local — já está no .gitignore
# ============================================================

# Supabase — encontre em: Settings > API no painel Supabase
REACT_APP_SUPABASE_URL=https://SEU_PROJETO.supabase.co
REACT_APP_SUPABASE_ANON_KEY=sua_anon_key_aqui
